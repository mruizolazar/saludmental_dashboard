# --- Framework web
Django==5.2.4

# --- PostgreSQL (binario: fácil de instalar en Windows/Mac/Linux)
psycopg2-binary==2.9.9

# --- Importadores de Excel / CSV (para comandos de importación)
pandas==2.2.3
openpyxl==3.1.5
numpy==2.0.2
python-dateutil==2.9.0.post0

# --- Configuración con variables de entorno
python-dotenv==1.0.1
